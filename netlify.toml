[build]
  command = "npm run build"
  functions = "functions"
  publish = "dist"

[dev]
  framework = '#custom'
  command = 'npm run dev:webpack'
  targetPort = 8079
  port = 5000
  publish = "dist"
  autoLanch = false